name: app
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_auth:
  provider:
  cloud_firestore:
  community_material_icon:
  flutter_masked_text:
  cloud_functions:
  intl:
  fl_chart:
  tuple:
  jiffy:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/netflix.png
    - assets/sbanken.png

